# Module 11 Notes — CI vs CD

## What I did that counts as Continuous Integration
(Write here: e.g., every push triggers an automatic build + test run, catching errors
early without anyone manually running `dotnet build` themselves.)


## What CI vs CD actually means, in my own words
- Continuous Integration:
- Continuous Delivery:
- Continuous Deployment:


## What I'd need to add for Continuous Deployment
(Write here: e.g., a deploy job that pushes the build artifact to a live environment
automatically, with no manual approval step.)
