// Exercise 2: Functions and Methods
// Concepts: methods, parameters (in, out, ref), default values,
// method overloading, local functions

using System;

class Program
{
    static void Main()
    {
        // TODO 1: Call a method Add(int a, int b) and an overloaded
        // Add(double a, double b) - demonstrate overload resolution.


        // TODO 2: Write a method TryParseAge(string input, out int age)
        // that returns bool and uses an 'out' parameter, similar to
        // int.TryParse. Call it and branch on the result.


        // TODO 3: Write a method Increment(ref int value) that increments
        // a variable by reference, and show the caller's variable change.


        // TODO 4: Write a method Greet(string name, string greeting = "Hello")
        // with a default parameter value; call it both with and without
        // the second argument.


        // TODO 5: Write a local function inside Main (e.g. int Square(int x))
        // and use it to compute squares of a few numbers.
    }

    // Add your Add(), TryParseAge(), Increment(), Greet() method
    // definitions below Main().
}
